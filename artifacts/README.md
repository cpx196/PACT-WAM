# PACT-WAM artifacts

`evaluate_results/` contains lightweight historical evaluation metadata copied
from the local FastWAM workspace. JSON result files preserve task IDs, success
statistics, timing information, and experiment-specific diagnostics; CSV/YAML
files preserve summaries and launch configurations.

Large rollout videos and model checkpoints are intentionally kept outside this
GitHub package.
